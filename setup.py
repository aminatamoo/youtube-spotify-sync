from setuptools import setup, find_packages

setup(
   name='youtube_spotify_sync',
   version='1.0',
   description='A module to sync a Youtube Folder with a Spotify Playlist',
   author='Aminat Amoo',
   author_email='aminaamoo@gmail.com',
   packages=find_packages()
   #install_requires=['bar', 'greek'], #external packages as dependencies
)
